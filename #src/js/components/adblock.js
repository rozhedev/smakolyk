import { STATE_LIST } from '../values';

// const detect = document.querySelector("#detect");
// const adblockpopup = document.querySelector(".adblock-popup");
// const button = adblockpopup.querySelector(".adblock-popup__btn");

// let adClasses = ["ad", "ads", "adsbox", "doubleclick", "ad-placement", "ad-placeholder", "adbadge", "BannerAd"];
// for (let item of adClasses) {
//     detect.classList.add(item);
// }

// let getProperty = window.getComputedStyle(detect).getPropertyValue("display");
// if (!adblockpopup.classList.contains(STATE_LIST.show)) {
//     getProperty == "none" ? adblockpopup.classList.add(STATE_LIST.show) : adblockpopup.classList.remove(STATE_LIST.show);
// }

// if(button) {
//     button.addEventListener("click", () => {
//         adblockpopup.classList.remove(STATE_LIST.show);
//     });
// }